# Chuzzy's Neovim Config

My Neovim plugins and bindings, configured with lazy.nvim.

## Language Servers
So far, this config depends on the following language servers:

* `ts_ls` (TypeScript)
* `tailwindcss`
